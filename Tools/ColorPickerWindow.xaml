<Window x:Class="OrbsWin.Tools.ColorPickerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="ColorPickerWindow"
        Width="180" Height="240"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        WindowStartupLocation="Manual">
    <Border Style="{StaticResource ShadcnCard}" Margin="8">
        <Grid Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header / Drag Bar -->
            <DockPanel Grid.Row="0" Margin="0,0,0,6" MouseLeftButtonDown="OnHeaderDrag">
                <TextBlock Text="Color Picker" Foreground="{StaticResource BrushTextPrimary}" FontSize="12" FontWeight="Bold" VerticalAlignment="Center"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <!-- Pin Button -->
                    <Button x:Name="PinButton" Click="OnPinClick" Foreground="{StaticResource BrushTextSecondary}" Background="Transparent" BorderThickness="0" FontSize="11" Margin="0,0,2,0" Cursor="Hand" ToolTip="Pin window (stay open)">
                        <TextBlock x:Name="PinIconText" Text="📌" Opacity="0.5"/>
                    </Button>
                    <!-- Close Button -->
                    <Button Content="✕" Click="OnCloseClick" Foreground="{StaticResource BrushTextSecondary}" Background="Transparent" BorderThickness="0" FontSize="11" Cursor="Hand"/>
                </StackPanel>
            </DockPanel>

            <!-- Color Swatch & Hex Display -->
            <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,8">
                <Border x:Name="ColorPreviewBox" Width="24" Height="24" CornerRadius="4" BorderBrush="{StaticResource BrushBorder}" BorderThickness="1" Margin="0,0,8,0">
                    <Border.Background>
                        <SolidColorBrush x:Name="ColorPreviewBrush" Color="Black"/>
                    </Border.Background>
                </Border>
                <TextBlock x:Name="HexCodeText" Text="#000000" Foreground="{StaticResource BrushTextPrimary}" FontSize="15" FontWeight="Bold" VerticalAlignment="Center"/>
            </StackPanel>

            <!-- Magnified Pixel Grid View -->
            <Border Grid.Row="2" CornerRadius="{StaticResource RadiusControl}" BorderBrush="{StaticResource BrushBorder}" BorderThickness="1" ClipToBounds="True" Background="Black">
                <Grid>
                    <Image x:Name="MagnifierImage" Stretch="Fill" RenderOptions.BitmapScalingMode="NearestNeighbor"/>
                    <!-- Center Crosshair reticle -->
                    <Border Width="8" Height="8" BorderBrush="#FFFF00" BorderThickness="1" HorizontalAlignment="Center" VerticalAlignment="Center" IsHitTestVisible="False"/>
                </Grid>
            </Border>

            <!-- Toast / Guidance Text -->
            <TextBlock x:Name="ToastText" Grid.Row="3" Text="Click to Copy | Esc to Cancel" Foreground="{StaticResource BrushTextSecondary}" FontSize="10" HorizontalAlignment="Center" Margin="0,6,0,0"/>
        </Grid>
    </Border>
</Window>
