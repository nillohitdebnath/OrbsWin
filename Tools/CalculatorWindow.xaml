<Window x:Class="OrbsWin.Tools.CalculatorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="CalculatorWindow"
        Width="220" Height="300"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        WindowStartupLocation="Manual">
    <Border Style="{StaticResource ShadcnCard}" Margin="8">
        <Grid Margin="12">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Header / Drag Bar -->
            <DockPanel Grid.Row="0" Margin="0,0,0,8" MouseLeftButtonDown="OnHeaderDrag">
                <TextBlock Text="Calculator" Foreground="{StaticResource BrushTextPrimary}" FontSize="14" FontWeight="Bold" VerticalAlignment="Center"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <!-- Pin Button -->
                    <Button x:Name="PinButton" Click="OnPinClick" Foreground="{StaticResource BrushTextSecondary}" Background="Transparent" BorderThickness="0" FontSize="12" Margin="0,0,4,0" Cursor="Hand" ToolTip="Pin window (stay open)">
                        <TextBlock x:Name="PinIconText" Text="📌" Opacity="0.5"/>
                    </Button>
                    <!-- Close Button -->
                    <Button Content="✕" Click="OnCloseClick" Foreground="{StaticResource BrushTextSecondary}" Background="Transparent" BorderThickness="0" FontSize="12" Cursor="Hand"/>
                </StackPanel>
            </DockPanel>

            <!-- Display -->
            <Border Grid.Row="1" Background="{StaticResource BrushBackground}" BorderBrush="{StaticResource BrushBorder}" BorderThickness="1" CornerRadius="{StaticResource RadiusControl}" Padding="8,6" Margin="0,0,0,10">
                <TextBlock x:Name="CalcDisplay" Text="0" Foreground="{StaticResource BrushTextPrimary}" FontSize="24" FontWeight="Bold" HorizontalAlignment="Right" VerticalAlignment="Center"/>
            </Border>

            <!-- Keypad -->
            <UniformGrid Grid.Row="2" Rows="5" Columns="4">
                <!-- Row 1 -->
                <Button Content="C" Click="OnClearClick" Margin="2" Background="{StaticResource BrushDestructive}" Foreground="White"/>
                <Button Content="±" Click="OnNegateClick" Margin="2"/>
                <Button Content="%" Click="OnOperatorClick" Tag="%" Margin="2"/>
                <Button Content="÷" Click="OnOperatorClick" Tag="/" Margin="2" Style="{StaticResource ShadcnPrimaryButton}"/>

                <!-- Row 2 -->
                <Button Content="7" Click="OnDigitClick" Tag="7" Margin="2"/>
                <Button Content="8" Click="OnDigitClick" Tag="8" Margin="2"/>
                <Button Content="9" Click="OnDigitClick" Tag="9" Margin="2"/>
                <Button Content="×" Click="OnOperatorClick" Tag="*" Margin="2" Style="{StaticResource ShadcnPrimaryButton}"/>

                <!-- Row 3 -->
                <Button Content="4" Click="OnDigitClick" Tag="4" Margin="2"/>
                <Button Content="5" Click="OnDigitClick" Tag="5" Margin="2"/>
                <Button Content="6" Click="OnDigitClick" Tag="6" Margin="2"/>
                <Button Content="-" Click="OnOperatorClick" Tag="-" Margin="2" Style="{StaticResource ShadcnPrimaryButton}"/>

                <!-- Row 4 -->
                <Button Content="1" Click="OnDigitClick" Tag="1" Margin="2"/>
                <Button Content="2" Click="OnDigitClick" Tag="2" Margin="2"/>
                <Button Content="3" Click="OnDigitClick" Tag="3" Margin="2"/>
                <Button Content="+" Click="OnOperatorClick" Tag="+" Margin="2" Style="{StaticResource ShadcnPrimaryButton}"/>

                <!-- Row 5 -->
                <Button Content="0" Click="OnDigitClick" Tag="0" Margin="2"/>
                <Button Content="." Click="OnDecimalClick" Margin="2"/>
                <Button Content="=" Click="OnEqualsClick" Margin="2" Style="{StaticResource ShadcnPrimaryButton}"/>
                <Button Content="⌫" Click="OnBackspaceClick" Margin="2"/>
            </UniformGrid>
        </Grid>
    </Border>
</Window>
