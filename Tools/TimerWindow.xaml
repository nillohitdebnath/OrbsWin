<Window x:Class="OrbsWin.Tools.TimerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="TimerWindow"
        Width="220" Height="260"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        WindowStartupLocation="Manual">
    <Border Style="{StaticResource ShadcnCard}" Margin="8">
        <Grid Margin="12">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header / Drag Bar -->
            <DockPanel Grid.Row="0" Margin="0,0,0,8" MouseLeftButtonDown="OnHeaderDrag">
                <TextBlock Text="Timer" Foreground="{StaticResource BrushTextPrimary}" FontSize="14" FontWeight="Bold" VerticalAlignment="Center"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <!-- Pin Button -->
                    <Button x:Name="PinButton" Click="OnPinClick" Foreground="{StaticResource BrushTextSecondary}" Background="Transparent" BorderThickness="0" FontSize="12" Margin="0,0,4,0" Cursor="Hand" ToolTip="Pin window (stay open)">
                        <TextBlock x:Name="PinIconText" Text="📌" Opacity="0.5"/>
                    </Button>
                    <!-- Close Button -->
                    <Button Content="✕" Click="OnCloseClick" Foreground="{StaticResource BrushTextSecondary}" Background="Transparent" BorderThickness="0" FontSize="12" Cursor="Hand"/>
                </StackPanel>
            </DockPanel>

            <!-- Digital Display -->
            <TextBlock x:Name="TimerDisplay" Grid.Row="1" Text="00:00" Foreground="{StaticResource BrushAccent}" FontSize="36" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>

            <!-- Presets -->
            <UniformGrid Grid.Row="2" Columns="3" Margin="0,0,0,8">
                <Button Content="5m" Click="OnPresetClick" Tag="300" Margin="2"/>
                <Button Content="10m" Click="OnPresetClick" Tag="600" Margin="2"/>
                <Button Content="25m" Click="OnPresetClick" Tag="1500" Margin="2"/>
            </UniformGrid>

            <!-- Control Buttons -->
            <Grid Grid.Row="3">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Button x:Name="StartPauseButton" Grid.Column="0" Content="Start" Style="{StaticResource ShadcnPrimaryButton}" Click="OnStartPauseClick" Margin="2" Padding="6"/>
                <Button Content="Reset" Click="OnResetClick" Grid.Column="1" Margin="2" Padding="6"/>
            </Grid>
        </Grid>
    </Border>
</Window>
