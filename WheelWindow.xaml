<Window x:Class="OrbsWin.WheelWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="WheelWindow"
        Width="400" Height="440"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        WindowStartupLocation="Manual">
    <Grid>
        <!-- Top Search Box Overlay -->
        <Border x:Name="SearchBoxBorder"
                HorizontalAlignment="Center"
                VerticalAlignment="Top"
                Margin="0,4,0,0"
                Padding="10,5"
                Background="#EE131315"
                BorderBrush="#606366F1"
                BorderThickness="1"
                CornerRadius="{StaticResource RadiusPill}"
                Visibility="Collapsed"
                Panel.ZIndex="10">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="🔍 " Foreground="{StaticResource BrushTextSecondary}" FontSize="12" VerticalAlignment="Center"/>
                <TextBlock x:Name="SearchQueryText" Text="" Foreground="{StaticResource BrushTextPrimary}" FontSize="13" FontWeight="SemiBold" VerticalAlignment="Center"/>
            </StackPanel>
        </Border>

        <!-- Canvas Container -->
        <Canvas x:Name="WheelCanvas" Width="400" Height="400" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0,40,0,0"/>
    </Grid>
</Window>
